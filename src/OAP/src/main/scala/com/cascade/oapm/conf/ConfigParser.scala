package com.cascade.oapm.conf
import java.util
object ConfigParser {

  def parseConfig(path: String): util.Map[String, String] = {
    val config = new util.HashMap[String, String]()

    // TODO - adding parse config file

    config
  }
}
